---
import Layout from '@/layouts/Layout.astro';
import Header from '@/components/common/Header.astro';
import Footer from '@/components/common/Footer.astro';
import { projects } from '@/data/projects';

export function getStaticPaths() {
  return projects.map((project, index) => ({
    params: { id: index.toString() },
    props: { project },
  }));
}

const { project } = Astro.props;
---

<Layout title={`${project.title} | ポートフォリオ`}>
  <Header />

  <main class="pt-20 lg:pt-24">
    <div class="mx-auto max-w-4xl px-4">
      <h1 class="mb-6 text-2xl font-bold md:mb-8 md:text-3xl lg:text-4xl">
        {project.title}
      </h1>

      <div class="mb-6 overflow-hidden rounded-md shadow-md md:mb-8">
        <img
          src={project.image}
          alt={project.title}
          class="h-52 w-full object-cover md:h-72 lg:h-96"
        />
      </div>

      <div class="mb-6 grid grid-cols-1 gap-6 md:mb-8 md:grid-cols-3 md:gap-8">
        <div class="md:col-span-2">
          <h2 class="mb-3 text-lg font-bold md:mb-4 md:text-xl">プロジェクト概要</h2>
          <p class="mb-4 text-sm md:mb-6 md:text-base">
            {project.description}
          </p>
          <p class="mb-4 text-sm md:mb-6 md:text-base">
            このプロジェクトでは、ユーザー体験を向上させるために最新のウェブ技術を活用しました。
            レスポンシブデザインを採用し、あらゆるデバイスで最適な表示を実現しています。
            また、パフォーマンスの最適化にも注力し、高速な読み込み時間を実現しました。
          </p>

          <h2 class="mb-3 text-lg font-bold md:mb-4 md:text-xl">主な機能</h2>
          <ul
            class="mb-4 list-inside list-disc space-y-1 text-sm md:mb-6 md:space-y-2 md:text-base"
          >
            <li>レスポンシブデザイン</li>
            <li>高速なパフォーマンス</li>
            <li>直感的なユーザーインターフェース</li>
            <li>アクセシビリティ対応</li>
            <li>SEO最適化</li>
          </ul>
        </div>

        <div class="border-border/50 rounded-xl border bg-secondary/30 p-4 md:p-6">
          <h2 class="mb-3 text-lg font-bold md:mb-4 md:text-xl">プロジェクト情報</h2>
          <div class="space-y-3 md:space-y-4">
            <div>
              <h3 class="text-xs font-medium md:text-sm">完成日</h3>
              <p class="text-sm md:text-base">{project.year}</p>
            </div>
            <div>
              <h3 class="text-xs font-medium md:text-sm">カテゴリー</h3>
              <p class="text-sm md:text-base">{project.tags[0]}</p>
            </div>
            <div>
              <h3 class="text-xs font-medium md:text-sm">使用技術</h3>
              <div class="mt-1 flex flex-wrap gap-1.5 md:mt-2 md:gap-2">
                {
                  project.tags.map((tag) => (
                    <span class="rounded-full bg-background px-2 py-0.5 text-xs md:px-3 md:py-1 md:text-sm">
                      {tag}
                    </span>
                  ))
                }
              </div>
            </div>
            <div>
              <h3 class="text-xs font-medium md:text-sm">役割</h3>
              <p class="text-sm md:text-base">フルスタック開発者</p>
            </div>
          </div>
        </div>
      </div>

      <div class="text-center">
        <div class="flex justify-center">
          <a href="https://sample.copm" class="flex rounded-lg bg-secondary px-2 py-1">
            <img src="/src/assets/github-icon-light.png" alt="GitHubのロゴ" class="" />
            <p class="text-sm">ソースコード</p>
          </a>
          <a href="https://sample.copm" class="flex rounded-lg bg-secondary px-2 py-1">
            <p class="text-sm">DEMO</p>
          </a>
        </div>
        <a
          href="/projects"
          class="-foreground inline-block rounded-lg bg-primary px-4 py-2 text-sm font-medium shadow-lg transition-opacity hover:opacity-90 md:px-6 md:py-3 md:text-base"
        >
          プロジェクト一覧に戻る
        </a>
      </div>
    </div>
  </main>

  <Footer />
</Layout>
