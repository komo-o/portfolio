---
interface Props {
  className: string;
}

const { className } = Astro.props;
---

<div
  class="inline-flex h-[79.2px] w-[79.2px] items-center justify-center rounded-md border border-secondary shadow-md"
>
  <i class={`text-4xl ${className}`}></i>
</div>
